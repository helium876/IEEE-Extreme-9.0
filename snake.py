def animals(course_p):
	ani = {}
	loop = len(course_p)
	for x in xrange(0,loop):
		if course_p[x] == "-"
			ani[course_p[x]] = "normal"
		elif isinstance(course_p[x],str)
			s = course_p[x]
			if s.isdigit() == True:
				ani[course_p[x]] = "bunny"
			else:
				ani[course_p[x]] = "snake"


n = int(raw_input())
course = []

course_p = {}
count = n**2
player = count - (n-1)

for x in xrange(0,n):
	i = str(raw_input())
	r = list(i)
	course.append(r)
	for q in xrange(0,n):
		course_p[count] = course[x][q]
		count = count - 1

print course_p
m = int(raw_input())
for v in xrange(0,m):
	if m == :
		pass